<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.picsar">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- access permission to set the wallpaper -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Picsar">
        <activity
            android:name=".ui.SearchPhotoDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".ui.ExploreActivity"
            android:exported="true" />
        <activity
            android:name=".ui.WallpaperActivity"
            android:exported="true" />
        <activity android:name=".ui.SearchPhotosActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>